# 项目状态

## 当前版本

- 阶段：V0
- 名称：Localogue
- 状态：设计与规范冻结
- 日期：2026-09-01

## 已确定

- 产品定位：本地优先、资料库优先的个人媒体收藏与元数据管理系统；
- 核心价值：资料治理 + 资料探索；
- V1 采用 JSON 作为 Canonical Library 的文件化持久化形式；
- CSV / XLSX 用于交换与批量编辑；
- V2 再迁移 SQLite；
- 使用 Repository 抽象隔离持久化实现；
- 支持 Work、Person、Maker、Label、Series、Genre、Tag、Asset、MediaFile、Evidence 等核心实体；
- 支持按演员、导演、系列、厂商、厂牌、时间、类型、标签、时长等组合筛选；
- 支持作品排序与人物详情页中的二次筛选；
- 演员资料包含正式名、中文映射、英文/罗马字、别名、旧艺名、曾用名、出道、引退、复出、状态、出生信息、身高、三围、简介、图片等；
- 元数据以日文原文优先，中文和英文作为映射；
- UI 支持日文、简体中文、英文；
- 主题支持浅色、深色、跟随系统；
- NFO 是重要输入/输出格式，但不是最终真相源；
- 在线 Provider / Connector 延后到 V3；
- AI 延后到 V4。

## V0 交付物

- 产品与架构文档；
- 数据模型文档；
- 受控词表；
- 导入与审核流程；
- 查询 / 筛选 / 排序 / 时间线规范；
- UI 信息架构；
- JSON / CSV / XLSX 规范；
- SQLite 迁移原则；
- 参考项目分析；
- ADR 决策记录；
- 示例数据。

## 下一阶段

V1 的目标不是先做“数据库”，而是先做出可实际体验的 File-backed Library：

1. JSON Repository；
2. 示例数据加载；
3. 作品海报墙 / 列表 / 表格；
4. 人物列表和高质量人物详情页；
5. Faceted Search；
6. 时间、时长和多维排序；
7. 三语言 UI 与元数据回退；
8. 导入与 Review 原型；
9. CSV / XLSX 批量交换。
