# Localogue 快速上手（小白向）

> Localogue 目前是 0.1.25 内部测试版。本指南描述目标流程；公开安装包和干净环境验收尚未完成。

## Localogue 是什么

Localogue 是一款本地优先的 AV 个人资料库与策展管理器。它扫描并关联本地媒体，叠加可信公共元数据与私人修订，并提供浏览、筛选、编辑、备份和委托播放。

它默认不移动、不重命名、不删除你的视频。公共 Catalog、私人资料和原始媒体彼此分开；界面里的修改不会改写只读公共资料。

## 开发版启动

需要 Node.js 22+、pnpm 11.x、Rust 和 Tauri 开发环境：

```bash
pnpm install
pnpm desktop:doctor
pnpm desktop:dev
```

如果已有 Windows 安装包，可以直接安装使用。当前安装包仍待干净环境验收，不应作为公开稳定版分发。

## 第一次使用

1. 打开 Desktop；
2. 在首次引导中选择一个或多个存放视频、NFO 和图片的内容目录；
3. 私人资料库使用应用建议的默认位置，通常无需手工选择；
4. 启动统一同步并观察 NFO、图片和媒体三个阶段；
5. 同步完成后进入“资料库”浏览作品；
6. 未识别、番号冲突或多版本文件在“导入与整理”中处理。

以后某个目录新增文件时，可以只对该目录执行同步。资料库也可以只显示一个、多个或全部已启用目录的内容。

## 播放与文件安全

作品关联本地视频后，可用系统关联程序打开。Localogue 当前不内嵌视频解码器；用户指定播放器与参数模板仍属于待完善项。

删除资料记录和删除原始视频是不同操作。涉及写入、删除、导入或恢复时，请先使用隔离测试资料库；自动检查通过不等于这些流程已经由用户实机验收。

## 当前限制

- 在线元数据采集 Adapter 尚未实现，现阶段主要使用本地 NFO、Portable Pack、公共 Catalog 和手工编辑；
- Windows 是首个开发与验收平台；
- macOS、Linux、Docker、自动更新和正式签名不能视为已支持；
- Web 是历史功能宿主，不再作为新用户推荐入口；
- 真实大目录、安装升级和恢复流程仍在 0.2 Beta 前验收。

问题定位与剩余验收范围见 [Desktop 全流程审核台账](docs/development/desktop-ux-audit.md)。
