# Localogue 快速上手（小白向）

> 本指南给**只想把 Localogue 用起来**的人看，不需要懂编程。
> 如果你是要参与开发，请直接看 `README.md` 和 `docs/README.md`。

---

## 一、先搞清楚：Localogue 是做什么的

Localogue 是一个**本地优先的私人媒体资料库**——更准确地说，它是一个帮你**整理、归类、检索你自己的影视元数据（标题、演员、厂商、系列、题材等）**的工具。

它和"xx 刮削器 / 播放器"最大的不同，请你先记住这三点：

| 它**是** | 它**不是** |
|---|---|
| 一个本地资料库，数据都在你自己的电脑上 | 不联网帮你自动抓取网络上的元数据 |
| 一个整理 / 审核 / 浏览工具 | 不内置视频播放器 |
| 一个"我的数据我做主"的归档系统 | 不上传、不分享你的任何资料 |

简单说：**你提供资料（例如别的工具导出的 `.nfo` 文件、自己的 JSON/表格），Localogue 帮你规范化、去重、审核、按各种维度浏览。**

> 它的核心卖点是"可信赖"：每一条资料都保留了**来源证据**和**修改历史**，你能随时查到"这条信息是谁、从哪里来的、改过没有"。这是它和同类工具最不一样的地方。

---

## 二、两条上手路线，选一条

| 路线 | 适合谁 | 需要准备什么 |
|---|---|---|
| **A. Web 模式**（推荐新手） | 只想先看看、体验一下 | 电脑装好 Node.js 22+ 和 pnpm |
| **B. 桌面端** | 想要独立窗口、本地文件扫描体验 | 安装包（见下方说明）或自行构建 |

### 路线 A：Web 模式（最快看到效果）

1. 确认已安装 **Node.js 22 或更高版本**，以及 **pnpm**（`npm i -g pnpm` 即可装）。
2. 在项目目录里执行：

   ```bash
   pnpm install
   pnpm dev
   ```

3. 浏览器打开：

   ```text
   http://localhost:3000
   ```

4. 打开后你**立刻就能看到一个内置的演示资料库**（里面是虚构示例数据，全程只读，随便逛、不用担心弄坏什么）。

   - 如果页面是空的，补一句命令把演示数据装进来：

     ```bash
     pnpm library:init:demo
     ```

   - 然后刷新浏览器即可。

### 路线 B：桌面端

桌面端是一个独立的窗口程序（基于 Tauri），体验更接近"正经软件"，支持直接扫描你电脑上的本地视频、NFO、图片。

- **如果你已经有安装包**（例如 `Localogue_x64-setup.exe`）：双击安装，打开后在「设置 → 资料库 → 添加示例库」就能自动生成一个可玩的示例库。
- **如果还没有安装包**：桌面端目前需要你自己用源码构建（需要安装 Rust / Tauri 工具链），正式公开发布还在准备中。开发模式下运行：

  ```bash
  pnpm desktop:doctor   # 先检查本机环境是否齐全
  pnpm desktop:dev
  ```

  > 新手建议**先用路线 A 的 Web 模式**体验，桌面端构建对电脑环境要求较高。

---

## 三、最快"看到东西"：示例库

不想配置任何东西就想看效果？两个办法二选一：

- **Web 模式**：按上面第二节装好，`pnpm dev` 后默认就有演示库；或运行 `pnpm library:init:demo`。
- **桌面端**：「设置 → 资料库 → 添加示例库」，会自动从内置资源生成一份带图片、人物、作品的示例库，点开就能浏览。

示例库里的作品、人物、厂商都是**虚构的**，专门用来让你熟悉界面，不会动你任何真实文件。

---

## 四、正式用起来：四步流程

当你准备好管理**自己的**资料时，按这四步走：

### 第 1 步：建立你的私人资料库

告诉 Localogue 把你的真实资料放在哪里：

- **Web 模式**：打开 `http://localhost:3000/settings`，设置"私人资料库"路径，例如：

  ```text
  D:\MyMediaLibrary
  ```

  保存后（文件存在 Git 忽略的 `.localogue/settings.json`），后续操作就会用到这个新位置。
  也可以用环境变量 `LOCALOGUE_LIBRARY_PATH` 指定，优先级更高（适合 Docker / NAS / 服务器）。

- **桌面端**：「设置 → 资料库 → 新建资料库」，填一个内容根目录即可，应用会自动管好其余细节。

### 第 2 步：把资料导进来

打开导入页面（Web 是 `/import`），支持这些格式：

- **NFO**：最常见。很多媒体管理工具会为视频生成 `.nfo` 文件，直接选文件夹导入即可。
- **JSON / CSV / XLSX**：你自己整理好的表格或数据文件。

导入后，这些数据**不会直接写进你的正式资料库**，而是先变成"证据（Evidence）"暂存起来——这是为了安全和可追溯。

### 第 3 步：审核并确认

打开审核页面（Web 是 `/review`）：

- 系统会把导入来的信息和你资料库里已有的内容做**逐字段比对**（相同 / 不同 / 只有证据里有 / 只有库里有）。
- 你可以决定：保留原有资料，还是采用新证据；发现重复就合并；拿不准就先放着。
- 确认无误后提交，数据才正式进入你的资料库，并自动留下**修改留痕**。

### 第 4 步：浏览与检索

- `/works`：作品库，支持**海报墙 / 列表 / 表格**三种视图，可按演员、厂商、系列、题材、年份、时长等任意组合筛选。
- `/browse`：分类浏览，按厂商、厂牌、系列、题材等维度总览。
- `/people`：人物库，看演员、导演及其作品。
- 筛选、排序条件都保存在网址里，可以刷新、复制、分享。

> 想看本地视频文件信息（时长、分辨率、编码）？去 `/media` 扫描你的视频目录，可选配置 `ffprobe`。这一步不是必须的。

---

## 五、我的资料从哪来？

Localogue **自己不联网抓取**数据，所以你需要先有"原料"：

- 如果你用别的媒体管理工具，它们通常会生成 `.nfo` 文件，这就是现成的导入原料。
- 你也可以自己整理 CSV / XLSX / JSON 再导入。
- 社区共享资料（Shared Pack）是**只读**挂载的：公共基础资料可以直接用，你本地的修改永远优先、不会被覆盖。

这正是 Localogue 的设计哲学：**数据主权在你手里，工具只帮你整理，不替你决定真相。**

---

## 六、常见问题

**Q：打开是空的 / 看不到演示库？**
A：运行 `pnpm library:init:demo` 再刷新；或确认 `pnpm install` 和 `pnpm dev` 都成功执行了。

**Q：为什么导入后资料库里没看到？**
A：导入默认只生成"证据"，需要到 `/review` 审核并提交后才会进入正式资料库。这是有意的安全设计。

**Q：它能自动从网上找演员、封面吗？**
A：不能，也不计划做。Localogue 不联网抓取，所有元数据由你提供并审核。

**Q：我的真实文件会被改动吗？**
A：不会。Localogue 默认零侵入，只读取、不修改你的视频和图片目录。

**Q：能换电脑迁移吗？**
A：可以。在 `/packs` 导出"个人资料包（Personal Pack）"，到新电脑导入即可；导入默认只补缺失、不覆盖。

**Q：界面语言？**
A：支持简体中文 / 日本語 / English，且"界面语言"和"元数据显示语言"可以分开设置。

**Q：想自己构建桌面端但报环境错误？**
A：先跑 `pnpm desktop:doctor` 看缺什么，详细前置条件见 `docs/desktop/tauri-prerequisites.md`。

---

## 七、下一步看哪里

- 想知道每个功能怎么用 → 看 `docs/` 下对应的 `ui/`、`import/`、`curation/` 等目录。
- 想知道"它为什么这么设计" → 看 `docs/product/principles.md` 和 `docs/product/scope.md`。
- 想参与开发 → 从 `README.md` 的"推荐学习顺序"开始。

祝你整理愉快。数据是你的，Localogue 只是帮你把它理顺。
