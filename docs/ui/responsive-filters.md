# 响应式筛选器设计

Localogue 的筛选条件很多，因此筛选器既要信息密度高，又不能因为控件过窄出现横向滚动。

## 桌面端

- 左侧筛选栏目标宽度约 320~350px；
- 作品区域占剩余空间；
- 日期、时长等短字段可双列；
- 长选项文字允许换行；
- 筛选栏只允许纵向滚动。

## 中小屏幕

当页面宽度低于约 860px：

- 筛选栏移到作品列表上方；
- sticky 取消；
- 日期、时长双列改为单列；
- 不再出现独立横向滚动。

## 原则

不要通过 `overflow-x: hidden` 粗暴掩盖所有问题。

真正要先解决的是 Grid/Flex 子项的最小宽度：

```css
min-width: 0;
grid-template-columns: minmax(0, 1fr);
```

只有在确认内容可以合理收缩后，才限制筛选容器不出现横向滚动。
