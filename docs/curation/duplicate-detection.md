# 重复检测

## 作品重复

V1 可按以下信号由强到弱判断：

1. 规范化番号完全一致；
2. 外部稳定 ID 一致；
3. 媒体文件 SHA-256 一致；
4. 标题 + 日期 + Maker 高度一致；
5. 多个弱特征组合。

弱匹配不得自动合并。

## 人物重复

优先依据：

- 正式名完全匹配；
- 已知旧艺名 / 别名映射；
- 外部稳定人物 ID；
- 出生日期、出道时间等辅助特征。

仅仅“名字相似”不能自动合并人物。

## Asset 重复

可使用 SHA-256 检测完全相同文件；未来可选增加感知哈希检测近似图片。
