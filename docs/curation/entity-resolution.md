# 实体匹配

实体匹配负责把导入数据中的字符串映射到 Canonical Entity。

## Person 匹配

示例输入可能出现：

- 日文正式名；
- 中文映射；
- Romanized；
- 旧艺名；
- 别名；
- 不同空格 / 字体写法。

处理顺序建议：

1. 已记录的稳定外部 ID；
2. 已存在的精确名称条目；
3. 规范化后的精确名称；
4. 多特征候选；
5. 人工 Review。

## 不允许

不得因为模糊字符串相似度高就静默合并人物。
