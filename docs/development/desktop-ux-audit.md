# Desktop 全流程审核台账

本轮审核日期：2026-09-11。自动检查通过只证明静态/构建边界，不能替代用户验收。

## 本轮已修改

- 作品/人物详情按实体隔离草稿；加载、失败与记录不存在时仍可返回。
- 标签管理串行执行修改，私人归属只读取一次集合。
- 关于页增加离线中日英教程，明确当前没有在线自动更新能力；长弹窗正文滚动、操作区保留。
- 首页卡片读取完整题材和标签名称，人物/分类入口与作品库一致。
- 收藏和评分读取/保存失败不再静默吞掉错误。
- 普通/成功消息在 4.5 秒周期末尾淡出；相同消息重新触发会重新计时；减少动态效果模式不播放动画。
- 作品编辑候选搜索覆盖人物所有姓名；取消会丢弃未保存字段、关系选择和暂存 Tag，收起仍保留草稿。
- 瀑布流自动预取和手动加载共用同步锁，避免同帧重复请求多批数据。
- 作品浏览返回快照保存 WorkQuery、普通页码或瀑布流批次、视图和滚动位置；返回后重新查询 Repository，再恢复原浏览位置。
- 人物编辑增加取消草稿；人物与作品表单保存期间锁定全部输入和相关操作，并用同步 Ref 阻止同帧重复提交。
- Presentation Preference 更新按 Private Library 共享队列执行“读取最新值 → 合并单字段补丁 → 写回”，收藏、评分、作品首图与人物头像不会再用旧整对象互相覆盖。

## 已进行的实机检查

通过 Windows 桌面窗口查看首页、关于页，打开并关闭帮助教程；在约 1182×852 窗口下确认正文有滚动条且底部关闭按钮可见。
开发热更新后曾出现语言 Context 失效导致的白屏；重新加载窗口后关于和帮助正常。该问题不能作为已修复项目关闭，仍需独立复现热更新链。
未在用户真实资料库中执行删除、还原或测试性写入。

## 后续顺序与未完成项

1. **浏览返回状态**：代码与自动检查已完成；仍需 Desktop 实机核查普通分页和瀑布流分别经过 Work → Person → Work 多级访问后逐级返回的位置恢复。当前状态为等待用户验收。
2. **编辑写入安全**：代码和自动检查已完成；人物取消、保存锁与偏好串行合并仍需在隔离测试库做实际写入专项验收，不使用用户真实资料测试。
3. **统一确认**：部分 Work、Person、Asset 删除仍用原生 confirm，需逐个迁移，不减少 Native 引用检查。
4. **标签分类**：空分类目前依赖首个已分配标签持久化；分类改名、完整排序呈现及失败恢复仍需完善。
5. **JavBus 三语来源库**：已有页面计数审计不是完整词表导入。888 为当时两页条目合计，不代表去重后的唯一词数；全量来源快照、去重、逐项日中英名称与语义审核尚未完成。不得宣称三语标签库已经完整。
6. **导入、备份、恢复**：在隔离示例库测试预览不写入、取消、失败与恢复路径，不拿用户媒体做破坏性测试。
7. **更新发行**：当前只说明手动更新；在线检查、签名与安装升级需要明确发布渠道设计，不能用“已是最新”占位。
8. **开发稳定性与性能**：复现语言 Context 热更新白屏；评估瀑布流前缀全量查询与 CSS columns 重排，不将“连续加载”误称为虚拟化。

## 交接时的测试边界

2026-09-11 收口重新执行并全部通过：`pnpm typecheck`、`pnpm desktop:typecheck`、`pnpm lint`、`pnpm validate:desktop`、`pnpm validate:platform`、`pnpm desktop:rust:check`、`pnpm desktop:build:webview`、`git diff --check`。这些检查覆盖本轮最终代码（含瀑布流请求锁），不是沿用旧结果。

本轮没有新增安装包或 release EXE，`desktop:build:webview` 只生成前端资源；不能把旧安装程序称为本轮产物。开发窗口已经启动，换电脑仍需重新启动本机环境。
用户要求收口提交后交接，因此后续任务暂停在本台账，不表示全流程审核完成。换机步骤及可复制提示词见 [交接文档](localogue-handoff.md)。

## 用户验收路径

运行 `pnpm desktop:dev`（与 release 数据隔离）后依次检查：首页卡片名称/跳转 → 作品编辑搜索旧艺名 → 修改后取消并重新打开 → 作品/人物关系返回 → 关于 → 帮助教程与日志。
实际写入、删除与恢复验收请使用独立测试资料库。当前状态：**等待用户验收**。
